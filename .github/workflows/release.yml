name: Build and Release Xonix

on:
  push:
    tags:
      - 'v*' # Trigger only when a version tag is created, e.g., v1.0.0

jobs:
  build:
    strategy:
      matrix:
        include:
          - arch: amd64
            os: ubuntu-latest
          - arch: arm64
            os: ubuntu-24.04-arm
    runs-on: ${{ matrix.os }}
    container:
      image: ${{ matrix.arch == 'arm64' && 'debian:trixie' || 'ubuntu:latest' }}
    permissions:
      contents: read
    
    steps:
      - name: Install Build Dependencies
        run: |
          apt-get update
          apt-get install -y git gnat gprbuild libncursesada-dev

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Xonix
        run: |
          mkdir -p bin
          gprbuild -P xonix.gpr
          mv bin/xonix bin/xonix-linux-${{ matrix.arch }}
          sha256sum bin/xonix-linux-${{ matrix.arch }} > bin/xonix-linux-${{ matrix.arch }}.sha256

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xonix-binaries-${{ matrix.arch }}
          path: bin/

  build-appimage:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          apt-get update
          apt-get install -y gnat gprbuild libncursesada-dev squashfs-tools wget

      - name: Build AppImage
        run: |
          cd appimage
          ./build-appimage.sh

      - name: Upload AppImage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: xonix-appimage
          path: bin/xonix-*-x86_64.AppImage

  release:
    needs: [build, build-appimage]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          pattern: xonix-*
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release-artifacts/xonix-*
          fail_on_unmatched_files: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
